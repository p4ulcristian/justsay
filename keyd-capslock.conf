# Add this line under the [main] section of /etc/keyd/default.conf, then
# `sudo keyd reload`. It remaps CapsLock to F13 so it no longer toggles caps
# and gives the iris-ptt daemon a clean hold signal.
#
# [main]
capslock = f13
