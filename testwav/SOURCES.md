# Test clips

`16k_fleurs_en_*.wav` are from the Google FLEURS `en_us` validation split
(CC BY 4.0), resampled to 16 kHz mono. Reference text:

- `16k_fleurs_en_0` when you call someone who is thousands of miles away you
  are using a satellite
- `16k_fleurs_en_1` now widely available throughout the archipelago javanese
  cuisine features an array of simply seasoned dishes the predominant
  flavorings the javanese favor being peanuts chillies sugar especially
  javanese coconut sugar and various aromatic spices
- `16k_fleurs_en_2` the u.n. also hopes to finalize a fund to help countries
  affected by global warming to cope with the impacts

`16k_piper_hu.wav` was synthesized with Piper (`hu_HU-anna-medium`) so there
is a Hungarian clip to test against:

- `16k_piper_hu` Szia, ez egy teszt. A diktáló alkalmazás magyarul is
  működik, remélem jól érted amit mondok.

Synthetic speech is harder than a real voice, so these are a pessimistic test.
